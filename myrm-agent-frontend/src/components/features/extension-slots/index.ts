/**
 * [INPUT]
 * - ./types
 * - ./useExtensionSlotStore
 * - ./ExtensionSlot
 *
 * [OUTPUT]
 * - ExtensionSlot, useExtensionSlotStore, types
 *
 * [POS]
 * 扩展插槽特性模块聚合出口。
 */

export * from './types';
export { useExtensionSlotStore } from './useExtensionSlotStore';
export { ExtensionSlot } from './ExtensionSlot';
