name: Server fractal docs

on:
  push:
    branches:
      - main
    paths:
      - 'myrm-agent-server/app/**'
      - 'myrm-agent-server/scripts/check_fractal_docs.py'
      - 'myrm-agent-server/scripts/check_file_line_budget.py'
      - 'myrm-agent-server/scripts/sync_arch_file_tables.py'
      - 'myrm-agent-server/scripts/ci/file_line_budget_baseline.txt'
      - 'myrm-agent-server/tests/architecture/data/fractal_header_baseline.txt'
      - '.github/workflows/server-fractal-docs.yml'
  pull_request:
    paths:
      - 'myrm-agent-server/app/**'
      - 'myrm-agent-server/scripts/check_fractal_docs.py'
      - 'myrm-agent-server/scripts/check_file_line_budget.py'
      - 'myrm-agent-server/scripts/sync_arch_file_tables.py'
      - 'myrm-agent-server/scripts/ci/file_line_budget_baseline.txt'
      - 'myrm-agent-server/tests/architecture/data/fractal_header_baseline.txt'
      - '.github/workflows/server-fractal-docs.yml'

concurrency:
  group: server-fractal-docs-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  fractal-arch:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    defaults:
      run:
        working-directory: myrm-agent-server
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Fractal docs gates (directory + strict headers baseline)
        run: |
          python3 scripts/check_fractal_docs.py
          python3 scripts/check_fractal_docs.py \
            --strict-headers \
            --header-baseline tests/architecture/data/fractal_header_baseline.txt
          python3 scripts/check_fractal_docs.py --no-stub
          python3 scripts/check_file_line_budget.py
