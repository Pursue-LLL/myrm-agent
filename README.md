# myrm-agent

MIT 开源产品仓（server + frontend + desktop）。

模块架构与开发说明见 [_ARCH.md](_ARCH.md)。

```bash
bash scripts/install.sh   # 安装依赖并注册 myrm CLI
myrm start                # 启动 WebUI → http://localhost:3000
```
