# tests/services/ssh_bridge/

## Overview
Unit tests for Multi-Host SSH asset vault, config importer, connection pool, SFTP manager, and Agent bridge.

## File Index

| File | Role | Description |
|------|------|-------------|
| `test_ssh_bridge.py` | Unit Test | Verifies vault CRUD, config parsing, connection probing, SFTP directory parsing, safety gate blocking, and log distillation. |
