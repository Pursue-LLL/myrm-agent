# services/kanban/service_mixins/

KanbanService mixin 组合域：`core` 单例与注入、`types` DTO、`ai_mixin` specify/decompose 薄壳、`board_task_mixin` 写操作、`query_dispatcher_mixin` 读查询与 dispatcher 生命周期。聚合出口见 `__init__.py`。上级 SSOT：`../_ARCH.md`。
