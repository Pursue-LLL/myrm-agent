# services/background 模块架构


## 架构概述

全局后台任务服务层。提供真正的零阻塞异步守护进程，用于承接在主事件循环中开销较高的耗时任务，避免 UI 自旋锁阻塞。

## 文件清单

| 文件 | 地位 | 职责 | I/O/P |
|------|------|------|-------|
| `daemon.py` | 核心 | `MaintenanceDaemon`，有界队列（maxsize=512）后台驻留任务执行器，幂等启动（asyncio.Lock），支持队列深度监控。负责会话结束时的记忆提取与存储落盘、自动技能提炼等。 | ✅ |
