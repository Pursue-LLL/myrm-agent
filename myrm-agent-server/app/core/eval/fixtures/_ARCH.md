# core/eval/fixtures 模块架构

评估（eval）体系的静态测试夹具数据。仅存放只读数据文件，不含可执行代码。

## 文件清单

| 文件 | 地位 | 职责 | I/O/P |
|------|------|------|-------|
| `locomo_smoke.json` | 数据 | Locomo 评测冒烟样本（问答对语料），供 eval 管线冒烟测试加载 | - |

## 依赖关系

- 被评估管线测试（`app/services/eval*` / eval 相关测试）按路径读取，无运行时反向依赖。