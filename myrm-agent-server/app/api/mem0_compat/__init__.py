"""Mem0 API compatibility layer.

Provides endpoints that conform to the Mem0 SDK wire protocol,
enabling zero-code-change migration for Mem0 SDK users by only
changing the `host` parameter to point to our server.
"""
